import { createAction } from 'redux-act';

export const getLastHistory = createAction('getLastHistory');

export const getHistory = createAction('getHistory');
export const setHistory = createAction('setHistory');
